## Level 3: CLI Calculator

This is a simple command-line calculator.

It allows the user to:
- Add
- Subtract
- Multiply
- Divide

The program uses functions for each operation and runs in a loop until the user chooses to quit.

It also handles division by zero errors.

Example:
Input: 5 + 3  
Output: 8